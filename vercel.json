{
  "scripts": {
    "install": "npm install --force",
    "build": "npm run build"
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "50mb"
      }
    }
  ]
}